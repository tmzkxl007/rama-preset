# -*- coding: utf-8 -*-
"""라마 한 편의 설계도. prep.py 가 편 폴더에 `episode.py` 로 만들어 준다 — 빈 칸을 채운다.

★읽고 시작해라: `docs/라마-지침서.md` (말투 §2 · 소재 §3 · 로고 §4), 보기: `examples/sb01_신병4_해병보고/episode.py`.
  규격은 `spec.py`, 벤치 근거는 `docs/벤치-라마의드라마-나레분석-20260918.md`.
"""
import spec
spec.ZOOM = 0.60            # 16:9 원본이면 0.60(가운데 1150px). ★세로 쇼츠 그림 칸이면 1.0 (prep.py 가 바꿔 준다)
spec.DLG_TAIL = 0.30
spec.NARR_PAD = 0.04
spec.TTS_TEMPO = 1.3        # "드라마" 보이스 1.3배 (사용자 확정)

SRC = "src.mp4"          # 편 폴더에 둔 소재 (세로 쇼츠면 그림 칸만 오린 것)
OUT = "ep01.mp4"         # 완성본 이름 — ★한글 이름은 로고 오버레이(ffmpeg)에서 죽을 수 있다. 영문·숫자로.

# ── 제목 두 행 (잘난체) ──────────────────────────────────
# 1행 흰색 = 장면 설명 / 2행 빨강(더 크다) = 인물·집단. 「~하는/~한 ○○」 꼴, 물음표·느낌표 없이.
#   보기: "보고 하나에 하루 다 가는" / "해병대 이병 최욱"   ·   "로맨스가 있었어?" / "백기태와 최유지"(참고 채널)
HEAD1 = ""
HEAD2 = ""

WORK = ""                # 작품명 — 업로드 세트 `* 작품:` 에 쓴다 (화면엔 안 찍힌다)
PLATFORM = ""            # 업로드 세트 "더 자세한 내용은 <플랫폼>에서 감상하는 걸 추천합니다!" 에 쓴다

# ── 아래 띠 로고 ────────────────────────────────────────
# 편 폴더에 둔 투명 PNG(흰 글자). 만드는 법은 지침서 §4. 없으면 None (아래 띠가 빈다).
LOGO = ("logo.png", 240, 1650)     # (파일, 높이px, 세로중앙y)

# 원본에 박힌 자막 띠의 위쪽 y (src.mp4 기준). prep.py 가 잡아 준다. 없으면 None.
HARDSUB_TOP = None

STYLE = "tome"           # 대사가 본문, 나레는 짧은 상황 설명
LIFT = 0                 # 어두운 소재면 10~60 (감마로 어두운 쪽만 올린다)

# ── 뼈대 ────────────────────────────────────────────────
# ("N", "문구 | 문구", [(시작,끝), ...])   나레 마디. `|` 덩이마다 컷 하나. 그림은 반드시 직접 붙인다.
# ("D", 시작, 끝, [("자막", 말시작, 말끝), ...])   원본 대사. 시각은 전부 asr 단어 시각. 한 덩이 15자 안쪽.
#
# ★나레 말투 (지침서 §2): 3인칭 과거 존댓말 **상황 설명** 한 문장 10~20자.
#   끝은 ~죠(닫기) / ~는데(다음 대사로 넘기기, 물음표 없이) / ~은·~는(주어만 깔고 대사가 서술어).
#   물음표 나레(~데? / ~은?) 금지. ~다/~습니다 금지. 인물은 이름·관계어. 마지막 마디만 반말 촌평 한 줄.
BLOCKS = [
    # ("N", "육군 앞에서 목소리가 작다고 | 혼이 난 해병 최욱은", [(0.0, 0.55), (0.6, 2.4)]),
    # ("D", 2.73, 12.68, [("이병 최욱", 2.81, 4.22), ("제가 타 군 인원 앞이라고", 4.25, 5.78)]),
    # ("N", "잘못 하나 고치는 데도 | 허락을 세 번이나 받죠", [(13.3, 14.4), (16.9, 18.0)]),
    # ...
    # ("N", "보고만 하다 하루 다 가겠네", [(47.4, 48.4), (48.5, 49.4)]),    # 촌평
]

# 0번 컷 = 이 편에서 가장 센 그림 (BLOCKS 첫 N 의 첫 컷과 같게 둔다)
CUTS = [
]

# 효과자막(분홍) — 톤이 안 맞으면 [] 로 둔다. 1장만 붙이는 건 위반.
EFFECTS = []
